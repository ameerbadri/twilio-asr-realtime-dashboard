"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Configuration {
    constructor(config) {
        let emsConfig = config.emsClient || {};
        const realm = config.realm || 'us1';
        const DEFAULT_EMS_HOST = `https://ems.${realm}.twilio.com`;
        const EMS_PATH = '/v1/token';
        const host = emsConfig.url
            || emsConfig.emsUri
            || emsConfig.emsUrl
            || DEFAULT_EMS_HOST + EMS_PATH;
        this.url = host;
    }
}
exports.default = Configuration;

import { ClientInfo } from './clientInfo';
/**
 * @classdesc Incapsulates network operations to make it possible to add some optimization/caching strategies
 */
declare class Network {
    productId: string;
    clientInfo: ClientInfo;
    config: any;
    transport: any;
    constructor(productId: string, clientInfo: ClientInfo, config: any, transport: any);
    private createHeaders();
    private backoffConfig();
    private executeWithRetry(request, retryWhenThrottled?);
    /**
     * Make a GET request by given URI
     * @Returns Promise<Response> Result of successful get request
     */
    get(uri: string): Promise<{}>;
    post(uri: string, body: Object, revision?: string, twilsockOnly?: boolean): Promise<{}>;
    put(uri: string, body: Object, revision: string): Promise<{}>;
    delete(uri: string): Promise<{}>;
}
export { Network };
export default Network;

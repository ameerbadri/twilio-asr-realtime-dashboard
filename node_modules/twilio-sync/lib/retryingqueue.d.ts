import { Task, JobExecutor } from './interfaces/executor';
declare class RetryingQueue implements JobExecutor {
    private queue;
    private isActive;
    constructor();
    private wakeupQueue();
    private pickNext();
    private pickSame(arg);
    private executeTask(task);
    add(task: Task, context: any, arg: any, errorHandler: any): Promise<{}>;
}
export { Task, JobExecutor, RetryingQueue };
export default RetryingQueue;
